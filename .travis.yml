language: python
python:
  - "3.7"

install:
  - pip install pipenv
  - pipenv install
script:
  - pipenv run python -m pytest --cov=./
  - pipenv run python -m doctest Scalpels.incorporator.py

after_success:
  - export CODECOV_TOKEN="b1fddf6e-743d-4fd0-8224-88186222b9aa"
  - bash <(curl -s https://codecov.io/bash)